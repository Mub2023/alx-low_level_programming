/*
 * file :10-add.c
 * Auth : mubarak musad atta
 */
#include "main.h"
/**
 * a + b adds two integers and returns the result
 * Retrun : sum the result
 */
int add(int a, int b)
{
	int sum;

	sum = a + b;
	return (sum);
}
