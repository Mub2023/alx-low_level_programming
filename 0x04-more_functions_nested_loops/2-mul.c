#include "main.h"
/**
 * mul - a function that multiplies two integers.
 * @a: first number.
 * @b: second number.
 * Return: 0 always
 */
int mul(int a, int b)
{
	int c = a * b;

	_putchar(c);
	return (0);
}
