#include<stdio.h>
#include <stdlib.h>
#include <time.h>
/**
 *main - Entry point
 *
 *Return: Always 0 (Success)
 **/
int main(void)
{
	char sa, e, q

	e = 'e';
		q = 'q';

	for(sa = 'a';sa <= 'z' ; sa++)
	{
		if (sa != e && sa != q)
		putchar(sa);
	i
	putchar('\n');
	return (0);
}
