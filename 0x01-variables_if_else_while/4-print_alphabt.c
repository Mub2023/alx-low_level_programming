#include<stdio.h>
#include <stdlib.h>
#include <time.h>
/**
 *main - Entry point
 *
 *Return: Always 0 (Success)
 **/
int main (void) 
{
	int (sa, e, q)
	    int e = 'e'
		    int q = 'q'

	for(sa = 'a';sa <= 'z' ; sa++)
	{(sa != e && sa != q)
		putchar(sa);
	}
	putchar('\n');
	return (0);
}
